Identified Issues and Improvements
Inconsistent Data Types:
- The id fields in most tables are defined as uuid, which is good for uniqueness. However, in tables like public.courses, public.timetable, public.attendance, public.study_materials, public.venues, public.live_sessions, public.study_folders, public.flashcard_reviews, public.study_sessions, and public.code_executions, the id is defined as text. For consistency and to ensure data integrity, it's recommended to use uuid for all id fields.
Missing Foreign Key Constraints:
- Many tables reference other tables (e.g., user_id, course_id, faculty_id, etc.) but do not have foreign key constraints defined. Adding these constraints is crucial for maintaining data integrity and ensuring that only valid references are made.
Potential Null Values:
- Some fields are defined as NOT NULL but have default values that could potentially lead to null values if not explicitly set. Reviewing these fields to ensure they align with the application's requirements is necessary.
Data Type Choices:
- The use of text for fields like email, name, and others might be too broad, considering the specific nature of these fields. Using more specific types like varchar with a defined length could be more appropriate.
Indexing:
- For tables that are frequently queried, especially on specific columns, adding indexes can significantly improve query performance.
Timestamps:
- The schema uses both timestamp with time zone and timestamp without time zone. For consistency and to handle time zones correctly, it's recommended to use timestamp with time zone for all timestamp fields.
Unique Constraints:
- Fields like email in the users table should have a unique constraint to prevent duplicate email registrations.
Cascade Actions:
- When defining foreign key constraints, consider specifying cascade actions (e.g., ON DELETE CASCADE) to handle the deletion of referenced records appropriately.
Example Corrections
For the public.users table, adding a unique constraint on the email field and ensuring the id is correctly referenced in other tables:

sql
CREATE TABLE public.users (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  email varchar(100) NOT NULL UNIQUE,
  -- ... other fields ...
  CONSTRAINT users_pkey PRIMARY KEY (id)
);
For the public.courses table, changing the id type to uuid and adding a foreign key constraint for faculty_id:

sql
CREATE TABLE public.courses (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  -- ... other fields ...
  faculty_id uuid,
  CONSTRAINT courses_pkey PRIMARY KEY (id),
  CONSTRAINT courses_faculty_id_fkey FOREIGN KEY (faculty_id) REFERENCES public.faculties(id)
);
Conclusion
The provided schema has a solid foundation but requires adjustments for consistency, data integrity, and performance. Implementing these changes will help ensure the database is robust and supports the application's functionality effectively.

Given the complexity and the number of tables involved, a thorough review of each table's structure and the relationships between them is essential. This process involves not just correcting the identified issues but also considering the application's specific needs and how the database design can best support those needs.

This response was generated by the McPherson AI Council. AI-generated content should be verified for critical




-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.users (
  id uuid NOT NULL,
  email text NOT NULL DEFAULT ''::text,
  name text NOT NULL DEFAULT 'User'::text,
  role text NOT NULL DEFAULT 'student'::text,
  department text NOT NULL DEFAULT 'Computer Science'::text,
  provider text NOT NULL DEFAULT 'email'::text,
  face_descriptor text NOT NULL DEFAULT ''::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  last_login timestamp with time zone NOT NULL DEFAULT now(),
  avatar_url text NOT NULL DEFAULT ''::text,
  admission_number character varying UNIQUE,
  admission_date date,
  expected_graduation_date date,
  cgpa numeric DEFAULT 0.000,
  academic_standing character varying DEFAULT 'Good'::character varying,
  is_active boolean DEFAULT true,
  staff_id character varying UNIQUE,
  faculty_id uuid,
  semester_id uuid,
  level character varying DEFAULT '100'::character varying,
  programme_id uuid,
  department_id uuid,
  CONSTRAINT users_pkey PRIMARY KEY (id),
  CONSTRAINT users_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id),
  CONSTRAINT users_programme_id_fkey FOREIGN KEY (programme_id) REFERENCES public.programmes(id)
);
CREATE TABLE public.courses (
  id text NOT NULL,
  code text NOT NULL DEFAULT ''::text,
  title text NOT NULL DEFAULT ''::text,
  level text NOT NULL DEFAULT '100'::text,
  units integer NOT NULL DEFAULT 3,
  department text NOT NULL DEFAULT ''::text,
  department_name text NOT NULL DEFAULT ''::text,
  college text NOT NULL DEFAULT ''::text,
  lecturer_id text NOT NULL DEFAULT ''::text,
  custom boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  faculty_id uuid,
  semester_id uuid,
  academic_year_id uuid,
  is_elective boolean DEFAULT false,
  status character varying DEFAULT 'active'::character varying CHECK (status::text = ANY (ARRAY['active'::character varying, 'archived'::character varying, 'coming_soon'::character varying]::text[])),
  CONSTRAINT courses_pkey PRIMARY KEY (id)
);
CREATE TABLE public.timetable (
  id text NOT NULL,
  course text NOT NULL DEFAULT ''::text,
  day text NOT NULL DEFAULT ''::text,
  time text NOT NULL DEFAULT ''::text,
  room text NOT NULL DEFAULT ''::text,
  lecturer text NOT NULL DEFAULT ''::text,
  type text NOT NULL DEFAULT 'lecture'::text,
  user_id text NOT NULL DEFAULT ''::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT timetable_pkey PRIMARY KEY (id)
);
CREATE TABLE public.attendance (
  id text NOT NULL,
  course_id text NOT NULL DEFAULT ''::text,
  course_name text NOT NULL DEFAULT ''::text,
  status text NOT NULL DEFAULT 'present'::text,
  method text NOT NULL DEFAULT 'manual'::text,
  date text NOT NULL DEFAULT CURRENT_DATE,
  user_id text NOT NULL DEFAULT ''::text,
  timestamp timestamp with time zone NOT NULL DEFAULT now(),
  location text NOT NULL DEFAULT ''::text,
  session_id text NOT NULL DEFAULT ''::text,
  location_lat double precision NOT NULL DEFAULT 0,
  location_lng double precision NOT NULL DEFAULT 0,
  clock_in_time timestamp with time zone,
  clock_out_time timestamp with time zone,
  duration_minutes integer,
  CONSTRAINT attendance_pkey PRIMARY KEY (id)
);
CREATE TABLE public.study_materials (
  id text NOT NULL,
  course_id text NOT NULL DEFAULT ''::text,
  title text NOT NULL DEFAULT ''::text,
  source_text text NOT NULL DEFAULT ''::text,
  summary text NOT NULL DEFAULT ''::text,
  key_points jsonb NOT NULL DEFAULT '[]'::jsonb,
  flashcards jsonb NOT NULL DEFAULT '[]'::jsonb,
  quiz jsonb NOT NULL DEFAULT '[]'::jsonb,
  created_by text NOT NULL DEFAULT ''::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  diagrams jsonb NOT NULL DEFAULT '[]'::jsonb,
  chat_history jsonb NOT NULL DEFAULT '[]'::jsonb,
  podcast_url text NOT NULL DEFAULT ''::text,
  improved_note text NOT NULL DEFAULT ''::text,
  folder_id text NOT NULL DEFAULT ''::text,
  CONSTRAINT study_materials_pkey PRIMARY KEY (id)
);
CREATE TABLE public.venues (
  id text NOT NULL,
  name text NOT NULL DEFAULT ''::text,
  room_code text NOT NULL DEFAULT ''::text UNIQUE,
  latitude double precision NOT NULL DEFAULT 0,
  longitude double precision NOT NULL DEFAULT 0,
  radius_meters integer NOT NULL DEFAULT 50,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  building_name character varying,
  floor_number integer,
  capacity integer DEFAULT 50,
  venue_type character varying DEFAULT 'Lecture Hall'::character varying,
  facilities ARRAY DEFAULT ARRAY[]::text[],
  is_active boolean DEFAULT true,
  CONSTRAINT venues_pkey PRIMARY KEY (id)
);
CREATE TABLE public.live_sessions (
  id text NOT NULL,
  timetable_id text NOT NULL DEFAULT ''::text,
  course_id text NOT NULL DEFAULT ''::text,
  course_name text NOT NULL DEFAULT ''::text,
  lecturer_id text NOT NULL DEFAULT ''::text,
  lecturer_name text NOT NULL DEFAULT 'Lecturer'::text,
  venue_id text NOT NULL DEFAULT ''::text,
  room text NOT NULL DEFAULT ''::text,
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  ended_at timestamp with time zone NOT NULL DEFAULT now(),
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'ended'::text])),
  CONSTRAINT live_sessions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.table_name (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  inserted_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  data jsonb,
  name text,
  CONSTRAINT table_name_pkey PRIMARY KEY (id)
);
CREATE TABLE public.notifications (
  id text NOT NULL,
  user_id text NOT NULL,
  type text NOT NULL DEFAULT 'info'::text,
  title text NOT NULL DEFAULT ''::text,
  message text NOT NULL DEFAULT ''::text,
  data jsonb NOT NULL DEFAULT '{}'::jsonb,
  link text NOT NULL DEFAULT ''::text,
  read boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT notifications_pkey PRIMARY KEY (id)
);
CREATE TABLE public.complaints (
  id text NOT NULL,
  user_id text NOT NULL DEFAULT ''::text,
  area text NOT NULL DEFAULT ''::text,
  title text NOT NULL DEFAULT ''::text,
  message text NOT NULL DEFAULT ''::text,
  status text NOT NULL DEFAULT 'open'::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  priority text NOT NULL DEFAULT 'medium'::text,
  CONSTRAINT complaints_pkey PRIMARY KEY (id)
);
CREATE TABLE public.chat_conversations (
  id text NOT NULL,
  user_id text NOT NULL DEFAULT ''::text,
  title text NOT NULL DEFAULT 'New Chat'::text,
  model text NOT NULL DEFAULT 'council'::text,
  context jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT chat_conversations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.chat_messages (
  id text NOT NULL,
  conversation_id text NOT NULL DEFAULT ''::text,
  role text NOT NULL DEFAULT 'user'::text CHECK (role = ANY (ARRAY['user'::text, 'assistant'::text])),
  content text NOT NULL DEFAULT ''::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT chat_messages_pkey PRIMARY KEY (id),
  CONSTRAINT chat_messages_conversation_id_fkey FOREIGN KEY (conversation_id) REFERENCES public.chat_conversations(id)
);
CREATE TABLE public.ai_config (
  provider text NOT NULL,
  api_key text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_config_pkey PRIMARY KEY (provider)
);
CREATE TABLE public.general_chat_messages (
  id text NOT NULL,
  sender_id text NOT NULL DEFAULT ''::text,
  message text NOT NULL DEFAULT ''::text,
  file_url text NOT NULL DEFAULT ''::text,
  file_type text NOT NULL DEFAULT ''::text,
  file_name text NOT NULL DEFAULT ''::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT general_chat_messages_pkey PRIMARY KEY (id)
);
CREATE TABLE public.study_folders (
  id text NOT NULL,
  name text NOT NULL DEFAULT ''::text,
  color text NOT NULL DEFAULT '#6366f1'::text,
  icon text NOT NULL DEFAULT 'folder'::text,
  created_by text NOT NULL DEFAULT ''::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT study_folders_pkey PRIMARY KEY (id)
);
CREATE TABLE public.flashcard_reviews (
  id text NOT NULL,
  material_id text NOT NULL DEFAULT ''::text,
  flashcard_id text NOT NULL DEFAULT ''::text,
  user_id text NOT NULL DEFAULT ''::text,
  ease_factor real NOT NULL DEFAULT 2.5,
  interval_days integer NOT NULL DEFAULT 0,
  repetitions integer NOT NULL DEFAULT 0,
  next_review date NOT NULL DEFAULT CURRENT_DATE,
  last_reviewed timestamp with time zone NOT NULL DEFAULT now(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT flashcard_reviews_pkey PRIMARY KEY (id),
  CONSTRAINT flashcard_reviews_material_id_fkey FOREIGN KEY (material_id) REFERENCES public.study_materials(id)
);
CREATE TABLE public.flashcards (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  material_id text NOT NULL DEFAULT ''::text,
  front text NOT NULL DEFAULT ''::text,
  back text NOT NULL DEFAULT ''::text,
  order_index integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT flashcards_pkey PRIMARY KEY (id),
  CONSTRAINT flashcards_material_id_fkey FOREIGN KEY (material_id) REFERENCES public.study_materials(id)
);
CREATE TABLE public.study_sessions (
  id text NOT NULL,
  material_id text NOT NULL DEFAULT ''::text,
  user_id text NOT NULL DEFAULT ''::text,
  session_type text NOT NULL DEFAULT 'review'::text CHECK (session_type = ANY (ARRAY['review'::text, 'quiz'::text, 'flashcards'::text, 'stream'::text])),
  score real NOT NULL DEFAULT 0,
  total_questions integer NOT NULL DEFAULT 0,
  correct_answers integer NOT NULL DEFAULT 0,
  stats jsonb NOT NULL DEFAULT '{}'::jsonb,
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  ended_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT study_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT study_sessions_material_id_fkey FOREIGN KEY (material_id) REFERENCES public.study_materials(id)
);
CREATE TABLE public.wallet_transactions (
  id integer NOT NULL DEFAULT nextval('wallet_transactions_id_seq'::regclass),
  user_id uuid NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
  amount numeric NOT NULL,
  type character varying NOT NULL CHECK (type::text = ANY (ARRAY['credit'::character varying, 'debit'::character varying]::text[])),
  description text NOT NULL DEFAULT ''::text,
  created_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT wallet_transactions_pkey PRIMARY KEY (id),
  CONSTRAINT wallet_transactions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.gamification_profiles (
  user_id uuid NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
  xp integer NOT NULL DEFAULT 0,
  current_streak integer NOT NULL DEFAULT 0,
  longest_streak integer NOT NULL DEFAULT 0,
  last_study_date timestamp without time zone,
  CONSTRAINT gamification_profiles_pkey PRIMARY KEY (user_id),
  CONSTRAINT gamification_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.gamification_badges (
  id integer NOT NULL DEFAULT nextval('gamification_badges_id_seq'::regclass),
  user_id uuid NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
  badge_id character varying NOT NULL DEFAULT ''::character varying,
  earned_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT gamification_badges_pkey PRIMARY KEY (id),
  CONSTRAINT gamification_badges_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.ai_embeddings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  content text NOT NULL DEFAULT ''::text,
  title text NOT NULL DEFAULT ''::text,
  type text NOT NULL DEFAULT 'general'::text,
  source_id text NOT NULL DEFAULT ''::text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  user_id uuid NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_embeddings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.ai_predictions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  risk_score double precision NOT NULL DEFAULT 0,
  academic_risk double precision NOT NULL DEFAULT 0,
  feature_vector jsonb NOT NULL DEFAULT '{}'::jsonb,
  factors jsonb NOT NULL DEFAULT '[]'::jsonb,
  user_id uuid NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
  predicted_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_predictions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.ai_insights (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  insight text NOT NULL DEFAULT ''::text,
  category text NOT NULL DEFAULT 'general'::text,
  severity double precision NOT NULL DEFAULT 0,
  read boolean NOT NULL DEFAULT false,
  user_id uuid NOT NULL DEFAULT '00000000-0000-0000-0000-000000000000'::uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_insights_pkey PRIMARY KEY (id)
);
CREATE TABLE public.faculties (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL UNIQUE,
  code character varying NOT NULL UNIQUE,
  description text DEFAULT ''::text,
  dean_id uuid,
  established_date date,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT faculties_pkey PRIMARY KEY (id)
);
CREATE TABLE public.programmes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL,
  code character varying NOT NULL UNIQUE,
  department_id uuid,
  faculty_id uuid,
  degree_type character varying NOT NULL DEFAULT 'Bachelor'::character varying,
  duration_years integer NOT NULL DEFAULT 4,
  description text DEFAULT ''::text,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT programmes_pkey PRIMARY KEY (id),
  CONSTRAINT programmes_faculty_id_fkey FOREIGN KEY (faculty_id) REFERENCES public.faculties(id)
);
CREATE TABLE public.academic_years (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL UNIQUE,
  start_date date NOT NULL,
  end_date date NOT NULL,
  is_current boolean DEFAULT false,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT academic_years_pkey PRIMARY KEY (id)
);
CREATE TABLE public.semesters (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  academic_year_id uuid NOT NULL,
  name character varying NOT NULL,
  type character varying NOT NULL CHECK (type::text = ANY (ARRAY['first'::character varying, 'second'::character varying, 'summer'::character varying]::text[])),
  start_date date NOT NULL,
  end_date date NOT NULL,
  registration_start_date date,
  registration_end_date date,
  exam_start_date date,
  exam_end_date date,
  is_current boolean DEFAULT false,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT semesters_pkey PRIMARY KEY (id),
  CONSTRAINT semesters_academic_year_id_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id)
);
CREATE TABLE public.grade_system (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  grade character varying NOT NULL UNIQUE,
  grade_point numeric NOT NULL,
  min_score integer NOT NULL,
  max_score integer NOT NULL,
  description character varying DEFAULT ''::character varying,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT grade_system_pkey PRIMARY KEY (id)
);
CREATE TABLE public.university_config (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  key character varying NOT NULL UNIQUE,
  value jsonb NOT NULL DEFAULT '{}'::jsonb,
  description text DEFAULT ''::text,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT university_config_pkey PRIMARY KEY (id)
);
CREATE TABLE public.enrollments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  course_id text NOT NULL,
  course_name text DEFAULT ''::text,
  semester_id uuid,
  academic_year_id uuid,
  enrolment_date date DEFAULT CURRENT_DATE,
  status character varying DEFAULT 'active'::character varying CHECK (status::text = ANY (ARRAY['active'::character varying, 'completed'::character varying, 'dropped'::character varying, 'incomplete'::character varying]::text[])),
  grade character varying DEFAULT ''::character varying,
  grade_point numeric DEFAULT 0.00,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  credit_units integer DEFAULT 0,
  CONSTRAINT enrollments_pkey PRIMARY KEY (id),
  CONSTRAINT enrollments_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT enrollments_semester_id_fkey FOREIGN KEY (semester_id) REFERENCES public.semesters(id),
  CONSTRAINT enrollments_academic_year_id_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id)
);
CREATE TABLE public.lecturer_courses (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lecturer_id uuid NOT NULL,
  course_id text NOT NULL,
  course_name text DEFAULT ''::text,
  semester_id uuid,
  role character varying DEFAULT 'instructor'::character varying CHECK (role::text = ANY (ARRAY['instructor'::character varying, 'assistant'::character varying, 'coordinator'::character varying]::text[])),
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT lecturer_courses_pkey PRIMARY KEY (id),
  CONSTRAINT lecturer_courses_lecturer_id_fkey FOREIGN KEY (lecturer_id) REFERENCES public.users(id),
  CONSTRAINT lecturer_courses_semester_id_fkey FOREIGN KEY (semester_id) REFERENCES public.semesters(id)
);
CREATE TABLE public.departments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL UNIQUE,
  code character varying NOT NULL UNIQUE,
  faculty_id uuid,
  description text DEFAULT ''::text,
  hod_id uuid,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT departments_pkey PRIMARY KEY (id),
  CONSTRAINT departments_faculty_id_fkey FOREIGN KEY (faculty_id) REFERENCES public.faculties(id)
);
CREATE TABLE public.gems (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  name character varying NOT NULL,
  description text DEFAULT ''::text,
  system_prompt text NOT NULL,
  model character varying DEFAULT 'turbo'::character varying,
  temperature numeric DEFAULT 0.3,
  is_public boolean DEFAULT false,
  is_active boolean DEFAULT true,
  usage_count integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT gems_pkey PRIMARY KEY (id),
  CONSTRAINT gems_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.user_memory (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  key character varying NOT NULL,
  value text NOT NULL,
  category character varying DEFAULT 'general'::character varying,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_memory_pkey PRIMARY KEY (id),
  CONSTRAINT user_memory_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.research_reports (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  query text NOT NULL,
  report text NOT NULL,
  sources jsonb DEFAULT '[]'::jsonb,
  depth character varying DEFAULT 'standard'::character varying,
  elapsed_ms integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT research_reports_pkey PRIMARY KEY (id),
  CONSTRAINT research_reports_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.code_executions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  language character varying NOT NULL,
  code text NOT NULL,
  output text,
  error text,
  elapsed_ms integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT code_executions_pkey PRIMARY KEY (id),
  CONSTRAINT code_executions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);





fix this schema and put in app
